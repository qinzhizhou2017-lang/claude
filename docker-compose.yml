version: "3.8"

services:
  feishu-bot:
    build: .
    container_name: feishu-doc-bot
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
