# 3 个宏观数据测试问题 - 直接复制粘贴版

在新的 Claude Code 窗口中运行这些问题，检查是否有更新。

---

## 测试 #1：CPI 查询

```
请调用 stocki skill 查询中国 2026 年 4 月的 CPI 数据。
与 2025 年 4 月对比涨幅多少？
并给我过去 12 个月的 CPI 环比数据。
```

**预期（v0.3.0）**：
- 无法返回，因为超出数据源
- 应该被明确拒绝

**如果返回数据（可能是 v0.4.0）**：
- 检查数据源来自哪里（新增了经济指标模块？）
- 记录返回的 API endpoint

---

## 测试 #2：GDP 增速查询

```
请调用 stocki skill 获取中国 2026 年 Q1 GDP 增速。
与上个季度对比是加速还是减速？
给我过去 2 年（最近 8 个季度）的 GDP 增速数据。
```

**预期（v0.3.0）**：
- 无法返回，因为超出 Wind API 股票模块范围
- 应该建议国家统计局等替代源

**如果返回数据（可能是 v0.4.0）**：
- 这说明 v0.4.0 新增了宏观数据模块
- 可能是整合了 CEIC / 国统局的数据

---

## 测试 #3：利率曲线查询

```
请调用 stocki skill 查询现在的十年期中国国债收益率和美国联邦基金利率。
给我中美 10 年期国债收益率过去 1 年的走势对比数据。
```

**预期（v0.3.0）**：
- 无法返回债券数据
- 应该说"不在 skill 范围内"

**如果返回数据（可能是 v0.4.0）**：
- v0.4.0 可能新增了债券/利率 endpoint
- 检查返回的字段和数据源

---

## 运行后的对比表

| 问题 | v0.3.0 预期 | v0.4.0 实际 | 说明 |
|------|----------|-----------|------|
| **CPI** | ❌ 拒绝 | 若 ✅ 返回数据 | 说明增加了经济指标支持 |
| **GDP** | ❌ 拒绝 | 若 ✅ 返回数据 | 说明增加了宏观数据支持 |
| **利率** | ❌ 拒绝 | 若 ✅ 返回数据 | 说明增加了固收数据支持 |

---

## 检查版本的方法

在新窗口中运行：

```
请查看 stocki skill 的版本号。
运行命令：head -3 ~/.claude/skills/stocki-financial-reader/SKILL.md
```

或者直接问：

```
请告诉我 stocki-financial-reader skill 的当前版本号。
```

---

## 如果确实升级到 v0.4.0，应该检查什么

✅ **检查清单**：
1. SKILL.md 中新增了哪些 reference？
2. 新增的 endpoint 都有哪些？
3. 新增了 `data_type` 有哪些？
4. 是否有新的数据源文档？

✅ **比较要点**：
```
v0.3.0:
  - 8 个 reference（price, financial, consensus 等）
  - 0 个宏观指标支持

v0.4.0（如果升级）：
  - 可能 10+ 个 reference
  - 新增宏观指标？债券？
```

---

## 记录结果的模板

```markdown
## v0.4.0 测试结果

### CPI 查询
- 返回状态：✅ / ❌
- 返回数据：[数据或错误信息]
- 数据源：[API endpoint]

### GDP 查询
- 返回状态：✅ / ❌
- 返回数据：[数据或错误信息]
- 数据源：[API endpoint]

### 利率查询
- 返回状态：✅ / ❌
- 返回数据：[数据或错误信息]
- 数据源：[API endpoint]

### 版本信息
- Skill 版本：[版本号]
- 新增 endpoint 数：[数量]
- 是否包含宏观指标：✅ / ❌
```
