# 用户误差反馈报告示例
## 超出 Stocki Skill 能力范围的 7 个典型问题

本文档提供了用户常见的超出 skill 能力的问题示例，可用于文档改进、FAQ 建设、或错误提示优化。

---

## 问题 1：选股/筛选需求

### 用户原始问题
```
"帮我找出 A 股中 PE < 20、ROE > 15%、市净率 < 1.5 的股票，
按 ROE 降序排列，只看最近 5 年上市的公司。"
```

### 为什么超出能力
- **缺失的 API**：Stocki skill 仅支持"按 symbol 查询"，不支持"按条件反向查询"
- **文档提示**：`references/industry-and-symbols.md` 明确标注：
  ```
  "stock-screening skill **not yet delivered** (upstream limitation)"
  ```
- **本质限制**：这需要 full-table scan 能力，不在 Wind API v3 datareader endpoint 范围内

### 如何向用户说明
```
❌ 无法实现："PE < 20 的股票" 这种条件筛选

✅ 替代方案：
1. 用户自行提供股票列表，我帮你批量查询这些股票的 PE/ROE/PB
2. 或访问专业选股平台（Wind 客户端、聚宽、掌上财经等）进行多因子筛选
3. 然后将筛选结果给我，我提供深度分析
```

---

## 问题 2：板块/概念聚合

### 用户原始问题
```
"白酒板块最近都涨了多少？
分别列出白酒、新能源、芯片概念股的平均 PE 和平均涨幅。"
```

### 为什么超出能力
- **缺失的功能**：无法将"白酒板块"这个集合名称 → 股票列表
- **文档提示**：`references/industry-and-symbols.md` 明确说：
  ```
  "白酒板块都有哪些 / AI 概念股都有哪些 (sector/concept **name** → symbol list)
  → **OUT-OF-SCOPE** (requires `match_collection`; this skill does not implement it)"
  ```

### 如何向用户说明
```
❌ 无法实现："白酒板块" 这个集合名称的自动解析

✅ 替代方案：
1. 用户手动告诉我白酒股的代码：600519、000858、603636...
2. 我批量查询这些股票的数据并计算平均值
3. 或查看 Wind 客户端的"板块"功能，它提供了这个映射
```

---

## 问题 3：策略回测/历史表现评估

### 用户原始问题
```
"我有一个选股策略：每个月月初买入前 10 大流动性最好的白酒股，
月底卖出。回测过去 5 年的历史表现，计算年化收益率、最大回撤、夏普比率。"
```

### 为什么超出能力
- **缺失的框架**：Stocki skill 只提供原始 OHLCV 数据，不包含回测框架
- **文档提示**：`references/price-history.md` 明确标注：
  ```
  "Strategy backtest performance → backtest skill **not yet delivered** 
  (upstream limitation, tracked separately)"
  ```
- **进一步限制**：即使有原始数据，期货连续合约的滚动日会产生 1-5% 价格跳跃，污染长期收益计算

### 如何向用户说明
```
❌ 无法实现：策略回测、风险指标计算

✅ 可做的：
- 提供每支股票的历史 adj_close 数据，用户自行在 Python/Excel 回测
- 计算简单指标：某只股票过去 1 年涨跌幅、区间最高/最低价
- 建议使用专业回测工具：vnpy、backtrader、掌上财经专业版等

⚠️ 注意：期货连续合约有滚动日污染，长期收益计算精度受限
```

---

## 问题 4：技术面分析/技术指标

### 用户原始问题
```
"茅台最近是不是 MACD 金叉了？
RSI 现在是多少？三条均线是什么状态？"
```

### 为什么超出能力
- **缺失数据**：Stocki skill 仅提供 OHLCV 和成交量，不包含任何技术指标
- **属于客户端计算**：即使有原始 K 线数据，LLM 也不应负责计算 MACD、RSI、布林带等

### 如何向用户说明
```
❌ 无法实现：技术指标计算

✅ 可做的：
- 提供历史 K 线数据（close, high, low, open, volume）
- 用户用 TA-Lib、pandas-ta 库自行计算
- 或访问看盘软件（同花顺、富途、东方财富）查看技术指标

💡 建议：技术面应结合基本面分析，单纯看指标容易被套
```

---

## 问题 5：实时宏观数据/经济指标

### 用户原始问题
```
"现在美联储基准利率是多少？
中国 CPI 最新数据？
全球 VIX 恐慌指数现在多少？"
```

### 为什么超出能力
- **数据源限制**：Stocki skill 聚合的只有股票/期货/指数数据，不包含宏观指标
- **更新频率不同**：经济数据通常月度/季度发布，不是交易日更新

### 如何向用户说明
```
❌ 无法实现：宏观经济指标查询

✅ 替代方案：
- 美联储利率：访问 https://www.federalreserve.gov
- 中国 CPI：访问国家统计局官网 http://www.stats.gov.cn
- VIX 恐慌指数：使用 stocki 查询 VIX 期货价格（如果支持）
- 国债收益率曲线：Bloomberg、Wind 客户端

💡 提示：可通过 BDI 波罗的海指数、南华工业指数等商品期货曲线间接了解经济周期
```

---

## 问题 6：美国市场深度分析

### 用户原始问题
```
"AAPL 最近 8 个季度的财务数据和 ROE、净利率趋势。
特斯拉的一致预期和分析师目标价多少？"
```

### 为什么超出能力
- **市场限制**：Stocki skill 对 US 市场支持不完整
  ```
  cn/hk:stock × {financial, indicator, market_cap, valuation} ✅
  us:stock × {market_cap} ✅
  us:stock × {financial, indicator, valuation}  ❌ 不支持
  ```
- **共识预期完全无**：`consensus-and-target` 仅覆盖 CN 市场

### 如何向用户说明
```
❌ 无法实现：US 股票财务报表、指标、一致预期

✅ 可做的：
- US 股票实时价格、市值
- 历史 K 线（OHLCV）

✅ 替代方案：
- 财务报表：SEC EDGAR（https://www.sec.gov/edgar）
- 一致预期：Yahoo Finance、Seeking Alpha、Facteus
- 专业分析：Bloomberg Terminal、FactSet

📌 技术原因：Wind API v3 datareader 对 US 市场的支持范围有限
```

---

## 问题 7：加密货币价格历史

### 用户原始问题
```
"比特币过去一年的日 K 线数据给我一份。
以太坊这一年涨了多少？"
```

### 为什么超出能力
- **实时价格**：Stocki realtime-quote 支持获取最新价格（freshness 可能是 `latest_close`）
- **历史数据**：`price-history` 的 crypto 市场完全不可用，返回 error
  ```
  metadata.warnings: "fetch failed: Missing config: database_url - crypto"
  ```

### 如何向用户说明
```
❌ 无法实现：加密货币历史价格查询

✅ 可做的：
- 实时查询 BTC、ETH 等当前价格（可能延迟 1-5 分钟）

✅ 替代方案：
- CoinGecko API：免费的历史价格数据
- Binance API：币安官方 K 线数据
- Kraken API：专业交易所数据
- ArcticDB、InfluxDB：自建时序数据库

📌 原因：Wind API 不包含加密货币历史数据源
```

---

## 问题 8（额外）：实时盘口/分时数据

### 用户原始问题
```
"茅台现在的五档盘口是什么？
过去一小时的分钟级 K 线给我看看，我想做 T+0。"
```

### 为什么超出能力
- **缺失字段**：Stocki 仅提供聚合的日 K 或快照，无分时、盘口、逐笔成交
- **市场限制**：日内高频交易数据不在 Wind API v3 public endpoints 范围内（可能在私有 API 中）

### 如何向用户说明
```
❌ 无法实现：实时五档盘口、分时数据

✅ 可做的：
- 实时成交价、成交量
- 日 K 线（OHLCV）
- 当日涨跌幅、振幅

✅ 替代方案：
- 股票 App：同花顺、富途、东方财富（有完整盘口）
- 专业交易终端：Wind 客户端、Bloomberg

📌 限制：日内交易需要低延迟数据，LLM 交互模式不适合
```

---

## 建议的文档修改方案

如果这份报告用于改进 Stocki skill 文档，建议：

### 1. 在 README.md 中增加 "能做什么 / 不能做什么" 对照表

```markdown
## 能力范围速查表

| 需求类型 | 支持度 | 替代方案 |
|---------|------|--------|
| 选股/筛选 | ❌ | 用户提供股票列表，我批量查询 |
| 板块聚合 | ❌ | 用户手动列出股票代码 |
| 策略回测 | ❌ | 提供原始数据，用户用 backtrader |
| 技术指标 | ❌ | 提供 OHLCV，用户用 TA-Lib |
| 宏观数据 | ❌ | 国家统计局、Federal Reserve 官网 |
| US 财务数据 | ❌ | SEC EDGAR |
| Crypto 历史 | ❌ | CoinGecko、Binance API |
| 盘口分时 | ❌ | 股票 App（同花顺、富途） |
```

### 2. 在 SKILL.md 中增加 "边界说明" 章节

```markdown
## 明确的边界（不支持）

- **选股/筛选**：无反向查询能力（无 match_symbols_by_metrics）
- **策略回测**：无回测框架（tracked as separate skill）
- **技术指标**：无 MACD、RSI 等；仅提供原始 OHLCV
- **宏观数据**：无 GDP、CPI、利率等经济指标
- **衍生品**：无期权、权证、转债
- **美国市场**：仅支持市值，无财务报表
- **加密货币**：实时价格可查，历史数据无法获取
```

### 3. 在各 Reference 的 "Boundaries" 部分补充说明

例如 `fundamentals-panel.md` 的 Boundaries：
```markdown
- Boundaries:
  - （原有内容）
  - 想按 "PE < 20" 筛股票 → 超出范围，需要选股 skill
  - 想回测这个 PE 指标过去 5 年 → 提供原始数据，用户自行计算
```

---

## 用于错误提示的建议文案

### 当用户问"PE < 20 的股票有哪些"时

```
⚠️ 无法完成此请求

原因：Stocki skill 仅支持"按股票代码查询数据"，不支持"按条件反向查询股票列表"。
这需要一个专门的"选股筛选"功能，目前还未实现。

💡 替代方案：
1. 在 Wind 客户端或掌上财经中进行多因子筛选
2. 将筛选结果（股票代码列表）告诉我
3. 我为你批量查询并分析这些股票的基本面、估值、预期等

需要帮助吗？可以给我一个股票列表，我会提供深度分析。
```

### 当用户问"过去 5 年的策略回测"时

```
⚠️ 无法完成此请求

原因：Stocki skill 不包含回测框架。它能提供历史价格数据，但不能自动执行：
- 定期调仓
- 费用/滑点计算
- 风险指标（最大回撤、夏普比率）等

🔧 替代方案：
- 提供你需要的股票和日期范围的历史数据
- 你使用 Python 回测库（vnpy、backtrader）自行实现
- 或使用专业回测平台（聚宽、掌上财经专业版）

需要帮助吗？告诉我股票代码和时间范围，我提供数据。
```

---

## 总结

这 7-8 个问题涵盖了最常见的超出 Stocki skill 能力的场景：

| # | 类别 | 本质限制 |
|---|------|--------|
| 1 | 选股/筛选 | 无反向查询 API |
| 2 | 板块聚合 | 无 match_collection 功能 |
| 3 | 策略回测 | 无回测框架 |
| 4 | 技术指标 | 无指标库 |
| 5 | 宏观数据 | 超出 Wind API 股票模块范围 |
| 6 | US 市场深度 | API 覆盖不完整 |
| 7 | Crypto 历史 | 数据源缺失 |
| 8 | 盘口分时 | API 不提供、交互模式不适合 |

建议：将这些作为常见问题列在文档中，配上友好的错误提示和替代方案，可以大大降低用户困惑。
